.header{style: header_styles}
  .drawer-toggle
    = image_tag 'icons/mobile-nav.png'

  - if current_user
    %a.sign-out{href: destroy_user_session_path, 'data-method' => :delete, rel: "nofollow"}
      =image_tag 'icons/logout.png'

  - else
    .sign-in
      %a.auth_link.nav-link{href: '/users/sign_in'} Sign in

  .logo
    %a{href: '/'} Wisr
